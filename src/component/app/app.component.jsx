import React from 'react';
import { Button } from '../button';
import { ActionBar } from '../action-bar';

const RecallApp = () => {
  return (
    <div>
      <ActionBar />
    </div>
  );
};

export { RecallApp };
